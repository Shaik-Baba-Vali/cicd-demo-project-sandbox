<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.mycompany</groupId>
    <artifactId>cicd-demo-project-sandbox</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>mule-application</packaging>

    <name>cicd-demo-project-sandbox</name>

    <!-- ================= PROPERTIES ================= -->
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <app.runtime>4.10.1</app.runtime>
        <mule.maven.plugin.version>4.6.1</mule.maven.plugin.version>

        <cloudhub2.environment>Sandbox</cloudhub2.environment>
        <cloudhub2.target>CloudHub 2.0</cloudhub2.target>

        <business.group.id>3265c39d-1b19-4158-828c-58479d052eea</business.group.id>
    </properties>

    <!-- ================= BUILD ================= -->
    <build>
        <plugins>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
                <version>3.2.0</version>
            </plugin>

            <plugin>
                <groupId>org.mule.tools.maven</groupId>
                <artifactId>mule-maven-plugin</artifactId>
                <version>${mule.maven.plugin.version}</version>

                <extensions>true</extensions>

                <configuration>

                    <!-- ================= CLOUDHUB 2.0 DEPLOYMENT ================= -->
                    <cloudHub2Deployment>
                        <uri>https://anypoint.mulesoft.com</uri>

                        <provider>Connected App</provider>

                        <connectedAppClientId>${connectedApp.clientId}</connectedAppClientId>
                        <connectedAppClientSecret>${connectedApp.clientSecret}</connectedAppClientSecret>
                        <connectedAppGrantType>client_credentials</connectedAppGrantType>

                        <businessGroupId>${business.group.id}</businessGroupId>

                        <environment>${cloudhub2.environment}</environment>
                        <applicationName>${project.artifactId}</applicationName>
                        <muleVersion>${app.runtime}</muleVersion>

                        <region>us-east-2</region>

                        <vCores>0.1</vCores>
                        <replicas>1</replicas>

                    </cloudHub2Deployment>

                    <!-- CI/CD -->
                    <skipMunitTests>true</skipMunitTests>

                </configuration>
            </plugin>

        </plugins>
    </build>

    <!-- ================= DEPENDENCIES ================= -->
    <dependencies>

        <dependency>
            <groupId>org.mule.connectors</groupId>
            <artifactId>mule-http-connector</artifactId>
            <version>1.11.0</version>
            <classifier>mule-plugin</classifier>
        </dependency>

        <dependency>
            <groupId>org.mule.connectors</groupId>
            <artifactId>mule-sockets-connector</artifactId>
            <version>1.2.7</version>
            <classifier>mule-plugin</classifier>
        </dependency>

    </dependencies>

    <!-- ================= REPOSITORIES ================= -->
    <repositories>

        <repository>
            <id>anypoint-exchange-v3</id>
            <name>Anypoint Exchange</name>
            <url>https://maven.anypoint.mulesoft.com/api/v3/maven</url>
            <layout>default</layout>
        </repository>

        <repository>
            <id>mulesoft-releases</id>
            <name>MuleSoft Releases Repository</name>
            <url>https://repository.mulesoft.org/releases/</url>
            <layout>default</layout>
        </repository>

    </repositories>

    <!-- ================= PLUGIN REPOSITORIES (AS PER DOCUMENTATION) ================= -->
    <pluginRepositories>

        <pluginRepository>
            <id>mulesoft-releases</id>
            <name>MuleSoft Releases Repository</name>
            <url>https://repository.mulesoft.org/releases/</url>
            <layout>default</layout>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>

    </pluginRepositories>

    <!-- ================= DISTRIBUTION MANAGEMENT (EXCHANGE PUBLISHING) ================= -->
    <distributionManagement>

        <repository>
            <id>anypoint-exchange-v3</id>
            <name>Anypoint Exchange</name>
            <url>
                https://maven.anypoint.mulesoft.com/api/v3/organizations/${business.group.id}/maven
            </url>
        </repository>

    </distributionManagement>

</project>
